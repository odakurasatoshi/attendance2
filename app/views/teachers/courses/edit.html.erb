<div class="container">
	<div class="row">
		<h1>ジャンル編集</h1>
			<%= form_for [:teachers,@course] do |f| %>
				<%= f.label :コース %>
				<%= f.text_field :name %>
				<%= f.submit '変更を保存する', class: "btn btn-primary"%>
			<% end %>
	</div>
</div>