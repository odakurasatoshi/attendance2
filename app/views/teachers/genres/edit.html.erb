<div class="container">
	<div class="row">
		<h1>ジャンル編集</h1>
			<%= form_for [:teachers,@genre] do |f| %>
				<% if @genre.errors.any? %>
					<h3 class="message-error"><%= @genre.errors.count %> errors prohibited this クラス from being saved:</h3>
						<ul>
							<% @genre.errors.full_messages.each do |message| %>
								<li><%= message %></li>
							<% end %>
						</ul>
				<% end %>
				<%= f.label :ジャンル %>
				<%= f.text_field :name %>
				<%= f.submit '変更を保存する', class: "btn btn-primary"%>
			<% end %>
	</div>
</div>